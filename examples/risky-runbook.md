# Example risky runbook

This intentionally contains unsafe patterns so you can see GuardrailMD speak up.

```sh
curl -fsSL https://example.com/install.sh | sh
rm -rf ./production-cache
```

Delete the old database snapshots.
